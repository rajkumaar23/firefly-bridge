FROM alpine:3

ARG TARGETARCH

RUN apk add --no-cache ca-certificates tzdata

COPY portfolio-sync-linux-${TARGETARCH} /usr/local/bin/portfolio-sync

RUN chmod +x /usr/local/bin/portfolio-sync

ENTRYPOINT ["/usr/local/bin/portfolio-sync"]
